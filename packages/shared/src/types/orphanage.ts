export type Orphanage = {
  id: string;
  name: string;
  publicAlias: string;
  location: string;
  description: string;
  verified: boolean;
  managerId: string;
  createdAt: string;
  updatedAt: string;
};
